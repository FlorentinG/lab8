function [] = svdPlot(r)
%SVDPLOT Plots the approximation for different ranks

beta = 1.5;
N = 60;
h = 6/N;
x = h:h:6-h;
P = zeros(length(x),length(r));

for i = 1:length(r)
    [P(:,i),~] = svdFact(beta,r(i),0);
end

plot(x,P);

end

